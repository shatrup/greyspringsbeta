import React from "react"
//import Resources from "../../../utils/Resources"
//import CssResource from "./CssResource"

class CssSection extends React.Component {
    render() {
        // var ext = Resources.external;
        // var int = Resources.internal;
        // var external = [ext.font.raleway, ext.css.fontawesome, ext.css.bootstrap];
        // var internal = [int.css.website, int.css.jQuery.bxslider, int.css.markdown.main, int.css.markdown.modest, int.css.carousel];
        // var resources = external.concat(internal)

        return (
            <React.Fragment>
                {/* {resources.map((value, ii) => <CssResource href={value} key={ii}/>)} */}
            </React.Fragment>
        );
    }
}

export default CssSection;