import React from "react"
//import Resources from "../../../utils/Resources"
//import ScriptResource from "./ScriptResource"

class ScriptsSection extends React.Component {
    render() {
        // var ext = Resources.external;
        // var int = Resources.internal;
        // var external = [ext.javascript.jQuery.main, ext.javascript.bootstrap, ext.javascript.jQuery.validate];
        // var internal = [int.javascript.main, int.javascript.bootstrap.tabcollapse, int.javascript.jQuery.scrollto, int.javascript.jQuery.bxslider, int.javascript.jQuery.tablesorter, int.javascript.Carousel];
        // var resources = external.concat(internal)

        return (
            <React.Fragment>
                {/* {resources.map((value, ii) => <ScriptResource src={value} key={ii} />)} */}
            </React.Fragment>
        );
    }
}

export default ScriptsSection;